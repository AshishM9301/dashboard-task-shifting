import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#111111",
      light: "#f6f6f6",
    },
    secondary: {
      main: "#2f2f2f",
      light: "#ffcb74",
    },
    paper: {
      main: "#fff",
    },
  },
});
